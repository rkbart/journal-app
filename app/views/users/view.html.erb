<h1>Sign Up</h1>

<%= render form_with(url: '/users') do |f| %>
    <p>
        <%= f.label :email %>
        <%= f.text_field :email %>
    </p>   
    <p>
        <%= f.label :password %>
        <%= f.password_field :password %>
    </p>
    <%= f.submit %>
<% end %>